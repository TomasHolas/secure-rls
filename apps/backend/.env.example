# Ollama endpoint (ADR 0005) - tailnet machine or localhost
OLLAMA_BASE_URL=http://localhost:11434

# JWT signing secret (ADR 0009) - REQUIRED, the app fails fast without it.
# Generate: openssl rand -hex 32
JWT_SECRET=
