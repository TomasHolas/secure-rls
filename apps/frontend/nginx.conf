server {
    listen 3002;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: every deep path is a client route, so unknown paths serve the shell.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
